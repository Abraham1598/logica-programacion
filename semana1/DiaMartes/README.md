# 🔁 Semana 1 – Día Martes: Ciclos avanzados

## ✅ Ejercicios realizados

1. **Sumar múltiplos de 3 entre 1 y 100**
   - Recorrido con `for`
   - Condición `i % 3 == 0`
   - Acumulación con `+=`

2. **Reversar una palabra ingresada por el usuario**
   - Recorrido inverso con `while`
   - Acceso por índice
   - Concatenación de caracteres

3. **Contar números positivos hasta que se ingrese uno negativo**
   - Ciclo `do-while` (o `while True` en Python)
   - Entrada controlada por condición
   - Contador de valores válidos

## 🧪 Lenguajes utilizados

- Java
- Python
- JavaScript

## 🔍 Comparación entre ciclos

| Lenguaje     | `for` loop         | `while` loop       | `do-while` loop       |
|--------------|--------------------|---------------------|------------------------|
| Java         | `for (int i...)`   | `while (cond)`      | `do { } while (cond)`  |
| Python       | `for i in range()` | `while cond:`       | ❌ no tiene `do-while` |
| JavaScript   | `for (let i...)`   | `while (cond)`      | `do { } while (cond)`  |

## 💡 Reflexión personal

> Hoy trabajé con ciclos más complejos y condiciones dentro de bucles.  
> Aprendí a controlar el flujo de ejecución, validar entradas y recorrer estructuras en reversa.  
> El `do-while` fue útil para garantizar al menos una ejecución.  
> Python no tiene `do-while`, pero se puede simular con `while True`.  
> Me gustó ver cómo la lógica se mantiene entre lenguajes, aunque la sintaxis varía.

## 📌 Próximo paso

- Aplicar ciclos en arrays y listas
- Resolver problemas con `break` y `continue`
- Usar ciclos anidados